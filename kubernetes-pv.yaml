apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-audio-storage
spec:
  capacity:
    storage: 6Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  local:
    path: /home/kt/Sem_6/RecSys/Project/darbar_audio/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - minikube
